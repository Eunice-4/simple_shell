#include "shell.h"

/**
 * split_line - Splits a line into tokens
 * @line: Line to split
 * Return: Null-terminated array of tokens
 */
char **split_line(char *line)
{
    /* Tokenize the line */
    /* Return the tokens */
}

