#include "shell.h"

/**
 * check_builtin - Checks if a command is a built-in
 * @args: Command to check
 * Return: 1 if built-in, 0 otherwise
 */
int check_builtin(char **args)
{
    /* Check if args[0] is a built-in command */
    /* If so, execute the built-in command */
    /* Return 1 if built-in, 0 otherwise */
}

